Front end repo for LVV

Local commands:

- `npm run build:dev` builds the web app in development mode.
- `npm run build` builds the web app in release mode.
- `npm run serve:dev` starts the local OJET dev server.
- `npm run codegen:clients` regenerates API clients.
